// Functions input(argument) - code - output()

const square = (num) => {
  return num * 2;
};
// let value = square(2);
// console.log(value);

// Challenge Area

const convertFahrenheitToCelsius = (fahrenheit) => (fahrenheit - 32) * (5 / 9);
const value = convertFahrenheitToCelsius(68);
console.log(value);
